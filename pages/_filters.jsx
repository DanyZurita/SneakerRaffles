import React from "react";

import { Container, Button } from "reactstrap";

export default function SneakerFilters() {
  return (
    <Container className="filters" fluid>
      <Button className="filter" color="dark">
        Raffle
      </Button>
      <Button className="filter" color="dark">
        Raffle
      </Button>
      <Button className="filter" color="dark">
        Raffle
      </Button>
      <Button className="filter" color="dark">
        Raffle
      </Button>
      <Button className="filter" color="dark">
        Raffle
      </Button>
    </Container>
  );
}
